import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
export default function LoginPage() { const { login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ email: "", password: "" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (e) => { e.preventDefault(); setLoading(true); setError(""); try { await login(form.email, form.password); navigate("/account"); } catch (err) { setError(err.message); } finally { setLoading(false); } }; return <section className="section-shell py-16"><form onSubmit={submit} className="mx-auto max-w-md rounded-[2rem] bg-white p-8 shadow-sm"><p className="eyebrow">B2B customer account</p><h1 className="mt-3 font-display text-4xl">B2B Customer Login</h1><p className="mt-4 text-sm leading-6 text-brand-emerald/70">Sign in to manage your business profile, quotes, samples and orders. <Link className="font-bold underline" to="/register">Register your business</Link>.</p><label className="mt-6 block text-sm font-bold">Email<input type="email" required value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-2 w-full rounded-xl border p-3" /></label><label className="mt-4 block text-sm font-bold">Password<input type="password" required minLength="8" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} className="mt-2 w-full rounded-xl border p-3" /></label><p className="mt-3 text-xs text-brand-emerald/55">Password recovery is not available yet. Contact Rizoura if you need account assistance.</p>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="mt-6 rounded-full bg-brand-gold px-6 py-3 font-bold">{loading ? "Signing in…" : "Sign in"}</button></form></section>; }
