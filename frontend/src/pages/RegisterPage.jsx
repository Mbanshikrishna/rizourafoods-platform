import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
const types = [["RESTAURANT", "Restaurant"], ["HOTEL", "Hotel"], ["CAFE", "Cafe"], ["CATERER", "Caterer"], ["CLOUD_KITCHEN", "Cloud kitchen"], ["DISTRIBUTOR", "Distributor"], ["INSTITUTION", "Institution"], ["RETAILER", "Retailer"], ["OTHER", "Other"]];
export default function RegisterPage() { const { register } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ name: "", email: "", phone: "", password: "", businessName: "", businessType: "RESTAURANT", gstNumber: "", city: "", state: "", pincode: "", estimatedMonthlyVolume: "" }); const [error, setError] = useState(""); const [done, setDone] = useState(false); const submit = async (e) => { e.preventDefault(); try { await register(form); setDone(true); } catch (err) { setError(err.message); } }; if (done) return <section className="section-shell py-20"><div className="mx-auto max-w-xl rounded-[2rem] bg-white p-9 text-center"><p className="eyebrow">Account created</p><h1 className="mt-4 font-display text-4xl">Your business account is awaiting approval.</h1><p className="mt-4 leading-7 text-brand-emerald/70">You can sign in and submit quote or sample requests. Customer-specific commercial pricing is not available until the business is approved.</p><button onClick={() => navigate("/account")} className="mt-7 rounded-full bg-brand-forest px-6 py-3 font-bold text-brand-sand">Go to account</button></div></section>; return <section className="section-shell py-14"><form onSubmit={submit} className="mx-auto grid max-w-3xl gap-4 rounded-[2rem] bg-white p-7 shadow-sm sm:grid-cols-2"><div className="sm:col-span-2"><p className="eyebrow">B2B customer account</p><h1 className="mt-3 font-display text-4xl">Register your business</h1></div>{[["name", "Full name", "text"], ["email", "Business email", "email"], ["phone", "Phone", "tel"], ["password", "Password (12+ chars, upper/lower/number)", "password"], ["businessName", "Business name", "text"], ["gstNumber", "GST number (optional)", "text"], ["city", "City", "text"], ["state", "State", "text"], ["pincode", "Pincode", "text"], ["estimatedMonthlyVolume", "Estimated monthly volume", "text"]].map(([name, label, type]) => <label key={name} className="text-xs font-bold uppercase tracking-wider text-brand-emerald/70">{label}<input required={name !== "gstNumber" && name !== "estimatedMonthlyVolume"} type={type} value={form[name]} onChange={(e) => setForm({ ...form, [name]: e.target.value })} className="mt-2 w-full rounded-xl border border-brand-gold/20 p-3 text-base normal-case tracking-normal" /></label>)}<label className="text-xs font-bold uppercase tracking-wider text-brand-emerald/70">Business type<select value={form.businessType} onChange={(e) => setForm({ ...form, businessType: e.target.value })} className="mt-2 w-full rounded-xl border border-brand-gold/20 p-3 text-base normal-case tracking-normal">{types.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label>{error && <p className="sm:col-span-2 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="sm:col-span-2"><button className="rounded-full bg-brand-gold px-6 py-3 font-bold text-brand-forest">Create B2B account</button><p className="mt-3 text-xs text-brand-emerald/55">Your business profile is manually reviewed. Registration does not grant wholesale pricing.</p></div></form></section>; }
