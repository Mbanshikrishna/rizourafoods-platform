import { Link, Navigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";

const segments = [["Restaurants", "Consistent ingredients for busy kitchens."], ["Hotels", "Bulk staples for high-volume food service."], ["Caterers", "Flexible commercial requirements and pack preferences."], ["Retailers", "Branded product and wholesale conversations."], ["Wholesalers", "Bulk procurement discussions."], ["Distributors", "Territory and portfolio enquiries."], ["Export buyers", "Bulk and private-label enquiries."], ["Private label", "Packaging and manufacturing discussions."]];
export function B2BPage() { return <section className="section-shell py-14 sm:py-20"><p className="eyebrow">For business</p><h1 className="mt-4 max-w-3xl font-display text-4xl sm:text-6xl">Ingredients for food businesses that need a dependable supply conversation.</h1><p className="mt-6 max-w-2xl leading-8 text-brand-emerald/75">Rizoura connects buyers with its rice, spice and masala portfolio through structured commercial enquiries. Share your requirement; availability, pricing and commercial terms are confirmed by the team.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/request-quote" className="rounded-full bg-brand-gold px-6 py-3 font-bold">Request B2B quote</Link><Link to="/register" className="rounded-full border border-brand-forest px-6 py-3 font-bold">Register business interest</Link></div><div className="mt-14 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{segments.map(([title, copy]) => <article key={title} className="premium-card p-5"><h2 className="font-display text-2xl">{title}</h2><p className="mt-2 text-sm leading-6 text-brand-emerald/70">{copy}</p><Link to={`/request-quote?segment=${encodeURIComponent(title)}`} className="mt-4 inline-block text-sm font-bold text-brand-forest underline">Discuss your requirement</Link></article>)}</div></section>; }
export function SimplePage({ title, eyebrow, children }) { return <section className="section-shell py-14 sm:py-20"><p className="eyebrow">{eyebrow}</p><h1 className="mt-4 max-w-3xl font-display text-4xl sm:text-5xl">{title}</h1><div className="mt-7 max-w-3xl space-y-5 leading-8 text-brand-emerald/75">{children}</div></section>; }
export function AccountPage() { const { session, logout } = useAuth(); if (!session) return <Navigate to="/login" replace />; return <SimplePage eyebrow="Team account" title="Signed in"><p>You are signed in as {session.email}. The current backend supports administrator, editor, viewer and distributor authentication only. Customer profiles, quotes and order history are not yet available.</p><button onClick={logout} className="rounded-full bg-brand-forest px-5 py-3 font-bold text-brand-sand">Log out</button></SimplePage>; }
